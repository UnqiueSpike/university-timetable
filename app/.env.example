# Step 1 is a static interface and requires no environment variables.
# Do not add real passwords, tokens, or keys here.
# Database and Better Auth variables will be defined when those integrations exist.
